// !$*UTF8*$!
//
// Illustrative only. This bundle is a speculative example, not a buildable
// application — the file exists because an Xcode project directory always
// contains one, and because the project directory is the ONLY root-level
// marker a native Apple app has. SwiftUI, UIKit and AppKit ship in the
// platform SDK and appear in no manifest, so a dependency-only heuristic
// classifies this app as having no user interface at all.
//
{
	archiveVersion = 1;
	objectVersion = 77;
	rootObject = "PLACEHOLDER — illustrative example, not a real Xcode project";
}
