{
  "$comment": "Verb tier manifest. The init bundler installs `core` by default; `extended` verbs install only when opted in via `spectastic init --with <verb>`. A command file whose verb is listed in neither tier defaults to core (fail-safe: a verb installs unless explicitly marked extended). Per specs/018-explain/plan.html D-002.",
  "core": [
    "principles",
    "spec",
    "plan",
    "tasks",
    "implement",
    "propose",
    "apply",
    "triage"
  ],
  "extended": [
    "explain"
  ]
}
