---
name: spectastic-concepts
description: Spectastic's own foundational concepts — spec-driven development, RFC 2119 conformance keywords, and the grounding discipline that binds every plan. Dogfoods the knowledge-corpus convention (051-knowledge-corpus) with a real, in-repo pack rather than a placeholder.
---

# spectastic-concepts

The corpus convention proven by use: a small, real `knowledge/` pack about spectastic's own domain, committed
in-repo like any downstream project's would be. Read `index.md` for the map, then pull a document from
`references/` as the work calls for it — the same agentic-search discipline any consuming corpus uses.
