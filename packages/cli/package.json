{
  "name": "@spectastic/cli",
  "version": "0.1.0-pre.3",
  "description": "Spectastic CLI. Validate spec-html files; emits human, JSON, and SARIF.",
  "type": "module",
  "bin": {
    "spectastic": "./bin/spectastic"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "bin", "_bundled"],
  "scripts": {
    "prebuild": "node scripts/prebuild.mjs",
    "build": "node scripts/prebuild.mjs && tsup",
    "test": "vitest run"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0",
    "@spectastic/schema": "workspace:*",
    "commander": "^12.1.0",
    "picocolors": "^1.1.0",
    "tinyglobby": "^0.2.0"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
