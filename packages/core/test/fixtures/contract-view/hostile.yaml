openapi: 3.0.0
info:
  title: hostile fixture (072-contract-embedded-view T-002)
  description: >
    <script>alert(1)</script>
    <img src=x onerror="alert(document.cookie)">
    <a href="javascript:alert(1)">click</a>
paths:
  /x:
    get:
      summary: exercises the escaping claim (D-004) against a real attack, not an assertion
